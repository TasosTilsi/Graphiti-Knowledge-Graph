{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); transcript=$(echo \"$INPUT\" | jq -r '.transcript_path'); session=$(echo \"$INPUT\" | jq -r '.session_id'); \"$CLAUDE_PROJECT_DIR\"/.venv/bin/graphiti capture --auto --transcript-path \"$transcript\" --session-id \"$session\"",
            "async": true,
            "timeout": 120
          }
        ]
      },
      {
        "command": "graphiti capture --auto --transcript-path \"$transcript_path\" --session-id \"$session_id\"",
        "async": true,
        "timeout": 10
      }
    ]
  }
}